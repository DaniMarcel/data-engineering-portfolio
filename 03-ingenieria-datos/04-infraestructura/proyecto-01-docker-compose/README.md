# 🐳 Docker Compose - Data Stack

Stack de servicios para data engineering.

**Start:**

```bash
docker-compose up -d
```

**Services:**

- PostgreSQL (port 5432)
- Redis (port 6379)
- Adminer UI (port 8080)

**Access:**

- Adminer: http://localhost:8080
- PostgreSQL: localhost:5432 (user: dataeng)

**Stop:**

```bash
docker-compose down
```
