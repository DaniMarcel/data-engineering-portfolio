# 🗄️ SQLAlchemy ORM

ORM para bases de datos relacionales con SQLAlchemy.

**Quick Start:**

```bash
pip install -r requirements.txt
python src/models.py
```

**Features:**

- Modelos con relaciones (Usuario, Producto, Orden)
- CRUD operations
- Migrations ready
- Works con SQLite, PostgreSQL, MySQL

**Conceptos:** ORM, relationships, sessions, queries
